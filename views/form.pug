extends layout

block content
    h1="Submit New Message"
    form(action="/new" method="POST")
        input(type="text" name="messageUser")
        input(type="text" name="messageText")
        button(type="submit") 
